digraph x64lens_modules {
  rankdir=LR;
  node [shape=box];

  main -> cli;
  main -> info;
  main -> mitigations;
  main -> gadgets;
  main -> analyze;

  info -> filemap;
  info -> elf64;
  info -> report_text;
  info -> errors;

  mitigations -> filemap;
  mitigations -> elf64;
  mitigations -> phdr;
  mitigations -> report_text;
  mitigations -> errors;

  gadgets -> filemap;
  gadgets -> elf64;
  gadgets -> phdr;
  gadgets -> scanner;
  gadgets -> patterns;
  gadgets -> classifier;
  gadgets -> scoring;
  gadgets -> report_text;
  gadgets -> report_json;
  gadgets -> arena;
  gadgets -> errors;

  analyze -> filemap;
  analyze -> elf64;
  analyze -> phdr;
  analyze -> scanner;
  analyze -> patterns;
  analyze -> classifier;
  analyze -> scoring;
  analyze -> report_text;
  analyze -> report_json;
  analyze -> arena;
  analyze -> errors;

  phdr -> bounds;
  phdr -> regions;
  scanner -> regions;
  patterns -> scanner;
  classifier -> patterns;
  scoring -> classifier;

  filemap -> syscalls;
  arena -> syscalls;
  elf64 -> bounds;
  report_text -> print;
  report_text -> hex;
  report_json -> print;
  report_json -> hex;
  errors -> print;
}
